class AuthEntity{

  final String? message;
  final bool? hasPass;
  final bool? force;
  final String? token;
  final bool? isLogin;

  AuthEntity({this.message, this.hasPass,this.force, this.token, this.isLogin});
}